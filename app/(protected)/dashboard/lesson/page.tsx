"use client";

export default function LessonPage() {
  return (
    <div className="space-y-4">
      <div className="space-y-1">
        <p className="text-sm font-semibold uppercase tracking-wide text-blue-600">Lessons</p>
        <h1 className="text-3xl font-bold text-gray-900 dark:text-gray-50">Manage lessons</h1>
        <p className="text-sm text-gray-600 dark:text-gray-400">
          I need the lessons table schema (column names/types) to wire up CRUD. Please share it and I will enable the full table with pagination,
          filters, and add/edit dialog like the other pages.
        </p>
      </div>
    </div>
  );
}
